{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "develop-watch",
      "type": "shell",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "command": "npm run dsw ${fileDirname}"
    },
    {
      "label": "develop",
      "type": "shell",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "command": "npm run ds ${fileDirname}"
    }
  ]
}
